import styled from 'styled-components';

export const AnalyticsLink = styled.a`
  display: flex;
  align-items: center;
  padding: 0px 0px 0px 4px;
  border-radius: 4px;

  svg {
    height: 16px;
  }

  path {
  }

  &:hover,
  &focus {
    path {
    }
  }
`;
